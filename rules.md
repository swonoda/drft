mainリポジトリへの直接コミットは禁止。

作業をするときは必ずブランチを作成し、ブランチ上で編集をする。

コミットはまとめる。PR作成後に変更する場合は変更のあったファイルをすべてまとめてコミットするが、最初のコミットとは分離する

コミット後、PRを作成し、変更内容をPRのDescriptionに記載する。

PRは勝手にマージしないこと


既存機能と同じ処理が必要な場合は、独自実装を増やさず、再利用できる既存コードを優先して使用する。
